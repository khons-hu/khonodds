import {getData} from '../lib/proxy.mjs';
export default async function handler(req,res){res.setHeader('Cache-Control','public, max-age=20, s-maxage=45');res.setHeader('X-Content-Type-Options','nosniff');if(req.method!=='GET'){res.setHeader('Allow','GET');return res.status(405).json({error:'Read-only endpoint'})}try{res.status(200).json(await getData(new URL(req.url,'https://local.invalid').searchParams))}catch(e){res.setHeader('Cache-Control','no-store');res.status(/Invalid|Unknown|valid public/.test(e.message)?400:502).json({error:e.name==='TimeoutError'?'Polymarket timed out. Try again shortly.':e.message==='fetch failed'?'Cannot reach Polymarket. Try again shortly.':e.message})}}
